// Curated YouTube videos per session, keyed by session id → [{ id, title }].
// Populated unit-by-unit from a YouTube search. Sessions without an entry
// show a "search on YouTube" link for the topic until curated IDs are added.
export const VIDEOS = {
  // Unit I — filled next pass
};
